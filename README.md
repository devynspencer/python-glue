# Glue

A Flask app to host a suite of personal plugins.

## Setup

Deploy everything with Docker:

```shell
docker compose up
```

Navigate to [the app](http://localhost:5000) (port 5000 by default) and get started.

## Documentation

Check out the [Flask documentation](https://flask.palletsprojects.com/en/2.2.x/) for the basics.
